---
// Fetch GitHub repo data at build time
async function fetchRepo(owner: string, repo: string) {
  try {
    const res = await fetch(`https://api.github.com/repos/${owner}/${repo}`, {
      headers: {
        'Accept': 'application/vnd.github.v3+json',
        'User-Agent': 'personal-ai-hub'
      }
    });
    if (!res.ok) return null;
    return await res.json();
  } catch {
    return null;
  }
}

// Fetch commands from ai-life-assistant repo
async function fetchCommands(owner: string, repo: string) {
  try {
    const res = await fetch(`https://api.github.com/repos/${owner}/${repo}/contents/commands`, {
      headers: {
        'Accept': 'application/vnd.github.v3+json',
        'User-Agent': 'personal-ai-hub'
      }
    });
    if (!res.ok) return [];
    const files = await res.json();
    return files
      .filter((f: any) => f.name.endsWith('.md') && f.name !== 'README.md')
      .map((f: any) => ({
        name: '/' + f.name.replace('.md', ''),
        url: f.html_url
      }));
  } catch {
    return [];
  }
}

const repos = [
  { owner: 'lout33', repo: 'ai-life-assistant', type: 'core' },
  { owner: 'lout33', repo: 'what-did-i-do', type: 'skill' },
  { owner: 'lout33', repo: 'writing-style-skill', type: 'skill' },
];

const repoData = await Promise.all(
  repos.map(async ({ owner, repo, type }) => {
    const data = await fetchRepo(owner, repo);
    return data ? {
      name: repo,
      description: data.description,
      stars: data.stargazers_count,
      url: data.html_url,
      type,
      language: data.language,
      updatedAt: data.updated_at
    } : null;
  })
);

const projects = repoData.filter(Boolean);
const coreProject = projects.find(p => p?.type === 'core');
const skills = projects.filter(p => p?.type === 'skill');

const commands = await fetchCommands('lout33', 'ai-life-assistant');

// Core files from the 4-file system
const coreFiles = [
  { name: 'AGENTS.md', desc: 'Operations & rules' },
  { name: 'SOUL.md', desc: 'Agent personality' },
  { name: 'USER.md', desc: 'Your profile' },
  { name: 'NOW.md', desc: 'Current state' },
];
---

<section id="registry" class="py-24 px-6 bg-zinc-100 dark:bg-zinc-900">
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold mb-4">The Ecosystem</h2>
      <p class="text-zinc-600 dark:text-zinc-400 max-w-xl mx-auto">
        Core system, skills, and commands. All open source.
      </p>
    </div>
    
    <!-- Core Project -->
    {coreProject && (
      <div class="mb-12">
        <h3 class="text-sm font-semibold mb-4 text-zinc-400 uppercase tracking-wider">Core</h3>
        <a href={coreProject.url} target="_blank" rel="noopener" 
           class="block p-8 rounded-2xl border-2 border-black dark:border-white bg-zinc-50 dark:bg-zinc-800 hover:bg-black dark:hover:bg-zinc-100 hover:text-white dark:hover:text-black transition-all duration-300 group">
          <div class="flex items-start justify-between mb-4">
            <div>
              <h4 class="text-2xl font-bold group-hover:text-white dark:group-hover:text-black">{coreProject.name}</h4>
              <p class="text-zinc-600 dark:text-zinc-400 group-hover:text-zinc-300 dark:group-hover:text-zinc-600 mt-2">{coreProject.description}</p>
            </div>
            <div class="flex items-center gap-1 text-zinc-500 group-hover:text-zinc-300 dark:group-hover:text-zinc-600">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
              </svg>
              <span class="font-mono">{coreProject.stars}</span>
            </div>
          </div>
          <div class="flex flex-wrap gap-2">
            {coreFiles.map(file => (
              <span class="px-3 py-1.5 rounded-full bg-black dark:bg-white text-white dark:text-black text-xs font-mono group-hover:bg-white dark:group-hover:bg-black group-hover:text-black dark:group-hover:text-white transition-colors">
                {file.name}
              </span>
            ))}
          </div>
        </a>
      </div>
    )}

    <!-- Commands -->
    {commands.length > 0 && (
      <div class="mb-12">
        <h3 class="text-sm font-semibold mb-4 text-zinc-400 uppercase tracking-wider">Commands</h3>
        <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4">
          {commands.map(cmd => (
            <a href={cmd.url} target="_blank" rel="noopener"
               class="p-4 rounded-xl border border-zinc-300 dark:border-zinc-700 bg-zinc-50 dark:bg-zinc-800 hover:border-black dark:hover:border-white hover:bg-black dark:hover:bg-zinc-100 hover:text-white dark:hover:text-black transition-all duration-200 group">
              <span class="font-mono group-hover:text-white dark:group-hover:text-black">{cmd.name}</span>
            </a>
          ))}
        </div>
      </div>
    )}

    <!-- Skills -->
    {skills.length > 0 && (
      <div class="mb-12">
        <h3 class="text-sm font-semibold mb-4 text-zinc-400 uppercase tracking-wider">Skills</h3>
        <div class="grid sm:grid-cols-2 gap-4">
          {skills.map(skill => skill && (
            <a href={skill.url} target="_blank" rel="noopener"
               class="p-6 rounded-xl border border-zinc-300 dark:border-zinc-700 bg-zinc-50 dark:bg-zinc-800 hover:border-black dark:hover:border-white hover:bg-black dark:hover:bg-zinc-100 hover:text-white dark:hover:text-black transition-all duration-200 group">
              <div class="flex items-start justify-between">
                <div>
                  <h4 class="font-bold text-lg group-hover:text-white dark:group-hover:text-black">{skill.name}</h4>
                  <p class="text-sm text-zinc-600 dark:text-zinc-400 group-hover:text-zinc-300 dark:group-hover:text-zinc-600 mt-1">{skill.description}</p>
                </div>
                <div class="flex items-center gap-1 text-zinc-500 group-hover:text-zinc-300 dark:group-hover:text-zinc-600">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
                  </svg>
                  <span class="font-mono text-sm">{skill.stars}</span>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    )}

    <div class="text-center mt-12">
      <a href="https://github.com/lout33/ai-life-assistant" target="_blank" rel="noopener" 
         class="inline-flex items-center gap-2 px-6 py-3 border-2 border-black dark:border-white font-medium rounded-full hover:bg-black dark:hover:bg-white hover:text-white dark:hover:text-black transition-all duration-300">
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
          <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
        </svg>
        View on GitHub
      </a>
    </div>
  </div>
</section>
