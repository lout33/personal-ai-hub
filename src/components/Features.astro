---
const features = [
  {
    title: 'Memory',
    description: 'Persistent context across all sessions. The agent remembers your goals, patterns, and history.',
    side: 'human'
  },
  {
    title: 'Challenge',
    description: 'Not a yes-machine. Calls out your patterns, quotes your words back, holds you accountable.',
    side: 'agent'
  },
  {
    title: 'Acts',
    description: 'Not just chat. Writes code, searches, creates files, executes. Operates alongside you.',
    side: 'both'
  }
];
---

<section id="features" class="py-24 px-6 bg-zinc-50 relative overflow-hidden">
  <!-- Brush accent decorations -->
  <div class="absolute top-12 left-8 w-20 h-1 bg-zinc-200 rounded-full transform -rotate-6"></div>
  <div class="absolute bottom-12 right-8 w-24 h-1 bg-zinc-200 rounded-full transform rotate-6"></div>
  
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">What makes it different</h2>
      <p class="text-zinc-600 max-w-xl mx-auto">
        Not a chatbot. A symbiotic agent that lives in your filesystem, sees patterns you miss, and operates with you.
      </p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-6">
      {features.map((feature, i) => (
        <div class={`p-8 rounded-2xl border transition-all hover:shadow-lg ${
          feature.side === 'human' 
            ? 'bg-white border-zinc-200 hover:border-zinc-300' 
            : feature.side === 'agent'
            ? 'bg-zinc-900 border-zinc-800 text-white hover:border-zinc-700'
            : 'bg-gradient-to-br from-white to-zinc-100 border-zinc-200 hover:border-zinc-300'
        }`}>
          <div class={`w-12 h-12 rounded-xl flex items-center justify-center mb-6 ${
            feature.side === 'human'
              ? 'bg-zinc-100'
              : feature.side === 'agent'
              ? 'bg-zinc-800'
              : 'bg-zinc-200'
          }`}>
            <div class={`w-3 h-3 rounded-full ${
              feature.side === 'human'
                ? 'bg-zinc-900'
                : feature.side === 'agent'
                ? 'bg-white'
                : 'bg-gradient-to-br from-zinc-900 to-zinc-400'
            }`}></div>
          </div>
          <h3 class="text-xl font-semibold mb-3">{feature.title}</h3>
          <p class={feature.side === 'agent' ? 'text-zinc-400' : 'text-zinc-600'}>{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
