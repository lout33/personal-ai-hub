---
const features = [
  {
    title: 'Memory',
    description: 'Persistent context across all sessions. The agent remembers your goals, patterns, and history.',
  },
  {
    title: 'Challenge',
    description: 'Not a yes-machine. Calls out your patterns, quotes your words back, holds you accountable.',
  },
  {
    title: 'Acts',
    description: 'Not just chat. Writes code, searches, creates files, executes. Operates alongside you.',
  }
];
---

<section id="features" class="py-24 px-6 bg-zinc-200 dark:bg-zinc-900 relative overflow-hidden">
  <div class="absolute top-12 left-8 w-20 h-1 bg-zinc-300 dark:bg-zinc-800 rounded-full transform -rotate-6"></div>
  <div class="absolute bottom-12 right-8 w-24 h-1 bg-zinc-300 dark:bg-zinc-800 rounded-full transform rotate-6"></div>
  
  <div class="max-w-6xl mx-auto">
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4 text-zinc-900 dark:text-white">What makes it different</h2>
      <p class="text-zinc-600 dark:text-zinc-400 max-w-xl mx-auto">
        Not a chatbot. A symbiotic agent that lives in your filesystem, sees patterns you miss, and operates with you.
      </p>
    </div>
    
    <div class="grid md:grid-cols-3 gap-6">
      {features.map((feature) => (
        <div class="p-8 rounded-2xl border transition-all hover:shadow-lg bg-zinc-100 dark:bg-zinc-800 border-zinc-300 dark:border-zinc-700 hover:border-zinc-400 dark:hover:border-zinc-600">
          <div class="w-12 h-12 rounded-xl flex items-center justify-center mb-6 bg-zinc-200 dark:bg-zinc-700">
            <div class="w-3 h-3 rounded-full bg-zinc-900 dark:bg-white"></div>
          </div>
          <h3 class="text-xl font-semibold mb-3 text-zinc-900 dark:text-white">{feature.title}</h3>
          <p class="text-zinc-600 dark:text-zinc-400">{feature.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
